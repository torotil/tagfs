#!/usr/bin/python

debug = True

class TagFileUtils

	# appends a number of tags to the tagfiles directory section
	def appendDirectoryTags(tagfile, taglist)
		return

	# appends a number of tags to a file
	def appendFileTags(tagfile, filetotag, taglist)
		return

	# removes a file and it's tag from the tagfile
	def removeFileFromTagfile(tagfile)
		return

	# removes one or more tags from a file entry
	def removeFileTagsFromTagfile(tagfile, file, taglist)
		return
	
	# removes one or more tags from the directory section of a tagfile
	def removeDirectoryTags(tagfile, taglist)

	# parses a tagfile and updates the db
	def parseTagfile(tagfile, db)
		return
